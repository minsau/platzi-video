import  React  from 'react';
import ReactDOM from 'react-dom';

const appContainer = document.getElementById('app');
const holaMundo = <h1>Hola Mundo! Modified</h1>;
ReactDOM.render(holaMundo, appContainer);